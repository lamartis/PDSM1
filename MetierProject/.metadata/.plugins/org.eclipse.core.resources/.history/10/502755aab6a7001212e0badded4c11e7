import java.io.FileOutputStream;
import java.io.InputStream;
import java.io.OutputStream;
import java.lang.reflect.Constructor;

import javax.naming.Context;
import javax.naming.InitialContext;

import com.generatedClasses.integration.ArchiveInfo;
import com.generatedClasses.integration.GetArchivesListResponse;
import com.generatedClasses.integration.SaveArchiveResponse;
import com.generatedClasses.presentation.Archive;
import com.generatedClasses.presentation.ConnectRequest;
import com.generatedClasses.presentation.DisconnectRequest;
import com.generatedClasses.presentation.ListArchivesRequest;
import com.generatedClasses.presentation.User;
import com.interfaces.M2iInterface;
import com.interfaces.P2mInterface;
import com.outils.Codeur;
import com.outils.DecodeurI2M;
import com.outils.DecodeurP2M;
import com.outils.ZipFileWritter;
import com.servlets.StartUp;
import com.traitement.integration.TraitementI;
import com.traitement.presentation.TraitementP;

public class Main {
	private InputStream resourceAsStream;

	public static void main(String[] args) throws Exception{
	/*	ZipFileWritter zip = new ZipFileWritter("C:/Users/saad/Desktop/NewPds/wtpwebapps/Serveur/accounts/" +"moZip");
		zip.addFile("Serveur/fichier1.jar");
		zip.close();
		*/
		
	}
	void ss(){

		InputStream s =  this.getClass().getClassLoader().getResourceAsStream("fichier1.jar);
	}
}
